# Server port
server.port=8080

# Database configuration (if applicable)
# Replace the placeholders with your actual database details
#spring.datasource.url=jdbc:mysql://localhost:3306/sunbasedata_db
#spring.datasource.username=db_username
#spring.datasource.password=db_password

# Hibernate settings (if using JPA and a database)
#spring.jpa.show-sql=true
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5Dialect

#Security settings (if using Spring Security)
# Add appropriate security configurations here

#CORS configuration (if required for frontend-backend communication)
#spring.servlet.cors.allowed-origins=http://localhost:3000
#spring.servlet.cors.allowed-methods=GET,POST,PUT,DELETE

# Other application-specific properties
# Add other properties as needed for your application

